import UIKit

class AddMovieViewController: UIViewController {
    // This controller is for the add screen which will have:
    // (screen will a label for a "Add a new movie to the list")
    // a textField for user to enter movie -> iboutlet
    // an add movie button - ibaction
    // and addMovie call to model to add to array
    // first guarding for bad entries from user
    
    // Outlet from textField here:
    
    @IBOutlet weak var textField: UITextField!
    
    // Action and function to add movie
    
    
    
    
    
    
    
    
    
    
    
}
